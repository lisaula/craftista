apiVersion: v1
kind: Service
metadata:
  name: {{ include "catalogue-chart.fullname" . }}
  labels:
    {{- include "catalogue-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "catalogue-chart.selectorLabels" . | nindent 4 }}
