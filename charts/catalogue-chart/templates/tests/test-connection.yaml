apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "catalogue-chart.fullname" . }}-test-connection"
  labels:
    {{- include "catalogue-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "catalogue-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
