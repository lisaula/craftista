apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "general-chart.fullname" . }}-test-connection"
  labels:
    {{- include "general-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "general-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
